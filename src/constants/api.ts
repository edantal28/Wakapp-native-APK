// Use the same backend URL as the web app
const BACKEND_BASE_URL = 'https://wakemeup-native.preview.emergentagent.com';

export const API_URL = `${BACKEND_BASE_URL}/api`;
export const BACKEND_URL = BACKEND_BASE_URL;
